export interface User {
  id: number;
  firstName: string;
  lastName: string;
  email: string;
  profilePicUrl?: string;
  thumbnailUrl?: string;
  timezone?: string;
  createdAt: string;
  updatedAt: string;
}
